node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vite
server/node_modules
server/.env

# Local-only daily work log (not for the repo)
daley.md

# Local-only Claude Code guidance (kept on disk for Claude, out of the repo)
CLAUDE.md

# Local-only Claude Code config & skills (kept on disk, out of the repo)
.claude/
